# TODO for Codex — v15 冲刺清单

## 目标
把当前 `v14` 从“逻辑已通、结果较稳的中文母稿”，推进到“在构念上更接近**数据要素利用**、在测度上更可复核、在标题上更有辨识度”的 `v15`。

**核心思想**：
- 不再只靠“关键词频率 + 更审慎写法”来撑住“数据要素利用”这五个字。
- 优先把测度从“谈到数据”推进到“数据被用于具体业务/治理场景”。
- 所有新增工作都要服务于一句更强的主张：**资本市场更奖励具有实质内容、业务嵌入和治理闭环的数据要素利用信息，而不是泛化的数字概念提及。**

---

## 总原则（不要违反）
1. `PriceDelay` 仍是唯一主因变量；`SYNCH` 继续只做辅助指标。
2. `DUkw` 仍是 baseline treatment；`DUllm` / `DUllm_lenstd` 仍是 semantic validation / depth measure。
3. 不要为了保住“数据要素利用”这五个字而回避不利结果。若新增检验削弱构念独立性，默认**降温写法**，而不是补更多花哨异质性掩盖问题。
4. 不要把“第二个 LLM 复核”写成“人工复核”。第二个 LLM 只能写成 **cross-model validation**。
5. 只要结果不够稳，就降到附录；不要再把弱异质性塞回摘要、Highlights 和结论。

---

# P0（最高优先级，必须先做）

## Task 0.1 重新归档上下文级 LLM 原始评分日志
### 目的
补足 `v14` 目前最大的技术短板：原始上下文级评分日志未完整保留，导致无法重建替代聚合指标。

### 要做什么
- 重新运行 LLM 打分流程，至少对 `v14` 当前样本对应的全部关键词命中上下文重新打分一次。
- 每条上下文必须保存以下字段：
  - `firm_id`
  - `year`
  - `keyword`
  - `context_id`
  - `context_text`
  - `prompt_version`
  - `model_name`
  - `temperature`
  - `raw_output`
  - `parsed_score`
  - `timestamp`
- 输出一份可复用的上下文级主表，例如：
  - `data/llm/context_scores_full.parquet`
  - `data/llm/context_scores_full.csv`

### 验收标准
- 能从这份上下文级表中**重建**当前的 `llmscore`、`DUllm`、`DUllm_lenstd`。
- 任意抽取 20 条上下文，都能追溯到原始 prompt 和原始输出。

---

## Task 0.2 构造更接近“数据要素利用”的新主测度：`DUevent`
### 目的
把构念从“出现了数据相关词”推进到“数据被用于具体业务/治理场景中的具体动作”。

### 定义思路
仅当同一上下文窗口中同时满足以下三类信息时，记为一次“数据要素利用事件”：
1. **数据对象**：如客户数据、交易数据、运营数据、库存数据、行为数据、供应链数据、风控数据等；
2. **动作**：如采集、清洗、建模、预测、匹配、推荐、识别、调度、优化、授权、脱敏、交易、入表等；
3. **场景**：如营销、供应链、库存、客服、授信、风控、定价、物流、合规、治理、产品经营等。

### 实现要求
- 先做一个 **rule-based 版本**，不要上来就做过重的复杂 NLP。
- 优先利用现有关键词窗口，不必重跑全文分句。
- 输出至少两个版本：
  - `DUevent_raw`：企业-年份的事件计数
  - `DUevent`：每万字标准化频率
- 记录规则词典与匹配逻辑到：
  - `docs/measure_duevent.md`

### 回归要求
至少跑以下回归：
1. `DUevent -> PriceDelay` 同期 OLS
2. `DUevent_{t-1} -> PriceDelay_t` 滞后 OLS
3. `DUevent` 与 `DUkw` 联合回归
4. `DUevent` 与 `GenericNarr` 联合回归

### 决策规则
- 若 `DUevent` 在滞后规格下显著为负，且相较 `DUkw` 更不受 `GenericNarr` 吞噬，则在 `v15` 中把它写成**更接近数据要素利用实质口径的增强指标**。
- 若结果不稳，只放附录，不替换主文 baseline。

---

## Task 0.3 基于五维词典构造“闭环/完整性”指标：`DUchain`
### 目的
让测度更贴近官方“数据资源持有—加工使用—产品经营/价值实现—治理合规”的数据要素链条，而不是单点式“数字化提及”。

### 具体变量
基于现有五维：
- `datastock`
- `datadev`
- `dataapp`
- `datavalue`
- `datagov`

至少构造以下变量：
1. `DUchain_count`：企业-年份命中的维度数（0–5）
2. `DUclosedloop`：是否至少同时命中以下三类：
   - 数据资源/存量类（`datastock`）
   - 应用/加工类（`dataapp` 或 `datadev`）
   - 治理/价值实现类（`datagov` 或 `datavalue`）
3. `DUbalance`（可选）：五维分布的反 HHI 或熵值，刻画“是否均衡而非只喊一个点”

### 回归要求
至少跑：
1. `DUchain_count_{t-1} -> PriceDelay_t`
2. `DUclosedloop_{t-1} -> PriceDelay_t`
3. 与 `DUkw` 联合回归

### 决策规则
- 若 `DUclosedloop` 或 `DUchain_count` 有稳健负向系数，则主文中可加入一句：
  - “更完整的数据要素利用链条与更快的价格发现相关。”
- 若只有方向一致但不稳，放扩展分析或附录。

---

## Task 0.4 构造更窄、更硬的核心口径：`DUcore`
### 目的
减少“数字化转型/智能化/信息化”这类泛词对“数据要素利用”构念的污染。

### 做法
在现有 73 个词基础上，构造一套更窄的核心词表，只保留更贴近数据要素制度与经营闭环的词：
- 权利/流通：数据确权、授权、流通、交易、产品经营、登记、定价
- 价值实现：数据资产、数据产品、数据变现、数据入表
- 治理合规：数据治理、数据安全、隐私保护、分类分级、脱敏、合规
- 业务嵌入：风险控制、需求预测、客户画像、库存调度、供应链优化、推荐、定价优化

明确删除或单独剥离的泛化词：
- 数字化转型
- 数字化
- 信息化
- 智能化
- 大数据
- 人工智能
- 智能制造
- 智慧城市

### 输出
- `DUcore_raw`
- `DUcore`
- `docs/measure_ducore.md`

### 回归要求
至少跑：
1. `DUcore_{t-1} -> PriceDelay_t`
2. `DUcore + GenericNarr`
3. `DUcore + DUkw`

### 决策规则
- 若 `DUcore` 显著且比 `DUkw_resid` 更稳，则在摘要/引言中把措辞从“更贴近数据利用”升级为“更贴近数据要素利用的核心经营/治理口径”。
- 若不稳，只保留为附录支持材料。

---

## Task 0.5 做章节限定口径：`DU_MDA` / `DU_Business` / `DU_RiskNotes`
### 目的
把测度从“全篇谈到了数据”推进到“关键经营章节谈到了数据如何被使用”。

### 优先章节
1. `MD&A / 经营情况讨论与分析`
2. `业务模式 / 主营业务 / 核心竞争力 / 研发与运营`
3. `风险因素 / 内控治理 / 重要事项 / 数据资源相关附注`

### 具体要求
- 基于年报章节标题做 rule-based 切分。
- 至少构造：
  - `DUkw_mda`
  - `DUkw_business`
  - `DUkw_risknotes`
- 如果 LLM 成本可承受，再构造对应的 `DUllm_lenstd_mda`。

### 回归要求
- 跑各章节限定指标的滞后规格。
- 比较哪一类章节的系数最稳、最符合“实质利用”解释。

### 决策规则
- 若 `MD&A` / `Business` 章节口径明显更稳，则主文把测度表述进一步收口到“年报中经营嵌入的数据要素利用信息”。

---

# P1（第二优先级：把 LLM 测度补到可外审）

## Task 1.1 重建替代聚合指标
在 `Task 0.1` 完成后，基于上下文级日志重建以下企业-年份聚合：
1. `llm_mean`
2. `llm_share_ge2`
3. `llm_topk_mean`（如 top 3 或 top 20% 的均值）
4. 现有 `llmscore_mode`

并分别构造对应的：
- `DUllm_mean`
- `DUllm_share`
- `DUllm_topk`

### 回归要求
- 至少跑 `PriceDelay` 的滞后规格。
- 与 `DUkw` 联合回归看是否保留额外解释力。

### 验收标准
- 在附录中能形成一张“聚合方式稳健性”总表。

---

## Task 1.2 补 cross-model validation（不是人工复核）
### 做法
- 在 120 条分层样本基础上，用第二个 LLM 复核一次。
- 必须固定 rubric，不许临时改 prompt 口径。

### 输出指标
- exact agreement
- within ±1 agreement
- weighted kappa
- confusion matrix

### 文字要求
- 只能写“cross-model validation”或“inter-model agreement”
- 不能写“人工复核”

---

## Task 1.3 做真正的人类复核（轻量版）
### 做法
只让人工看**最难的样本**，降低工作量：
1. 泛化词窗口
2. 两个模型分歧窗口
3. 0/1 边界窗口
4. 1/2 边界窗口

样本量建议：
- 最低 60 条
- 理想 100–150 条

### 输出指标
- exact agreement
- weighted kappa
- confusion matrix
- 代表性争议案例 3–5 个

### 验收标准
- 附录中明确区分：
  - `cross-model validation`
  - `human adjudication`

---

# P2（第三优先级：把“新意”做出来）

## Task 2.1 强化“概念提及 vs 实质利用”的主故事
### 目的
把现在的 `WashGap` 扩展为更完整的主叙事，而不是只做一个扩展表。

### 必做回归
1. `WashGap_{t-1} -> PriceDelay_t`
2. `BroadShallow_{t-1} -> PriceDelay_t`
3. `DUkw_{t-1} + DUllm_lenstd_{t-1}` 联合回归
4. 若 `DUevent` 做出来，再跑 `DUevent + DUkw`
5. 若 `DUchain` 做出来，再跑 `DUchain + DUkw`

### 写法要求
若结果成立，把主文中的一句贡献升级为：
- “资本市场更奖励具有实质内容和经营嵌入的数据要素利用信息，而非单纯更频繁的概念性提及。”

---

## Task 2.2 做五维价值链分解
### 目的
回答“什么样的数据要素利用更能进入价格”。

### 要做什么
- 分别构造五维强度：
  - `DU_stock`
  - `DU_dev`
  - `DU_app`
  - `DU_value`
  - `DU_gov`
- 用同一滞后规格分别回归 `PriceDelay`。

### 预期
重点看以下维度是否更强：
- `dataapp`
- `datavalue`
- `datagov`

### 决策规则
- 若应用/治理/价值实现维度更强，则主文可以更有底气地把题目和摘要往“数据要素利用”而不是泛化“数字化”上靠。

---

## Task 2.3 只做一组真正有新意的异质性：可信度异质性
### 推荐优先顺序
1. `Big4` 审计
2. 内控质量（若数据可得）
3. 董事会/高管数字背景（若现成数据可得，否则不做）

### 理论逻辑
复杂的数据要素利用信息只有在披露可信度更高时，才更容易被资本市场及时吸收。

### 回归要求
- 只做一组最强的，不要再堆很多普通异质性。
- 必须用交互项或正式组间检验，不要只报分组系数。

### 决策规则
- 若结果只到 10% 水平，不进摘要。
- 若不稳，直接进附录或删除。

---

## Task 2.4 可选异质性：`datagov × post2021` 或隐私敏感行业
### 目的
把“数据要素利用”进一步往治理/合规价值上推进。

### 做法
优先做以下其中一个：
1. `datagov × post2021`
2. 隐私/消费者数据敏感行业分组

### 注意
- 这是锦上添花，不是主线。
- 若数据清洗麻烦，就不要抢占 P0/P1 时间。

---

# P3（文稿同步修改）

## Task 3.1 摘要、Highlights、引言同步改写
### 改写原则
- 若 `DUevent / DUchain / DUcore` 中至少有一个成立，则摘要前两句必须体现“从泛化数字叙事推进到更接近数据要素利用实质口径”。
- 若这些新增指标不够稳，则继续沿用 `v14` 的审慎写法，不夸大。
- `ForecastDisp` 继续只写成 supplementary evidence。
- 弱异质性不得回摘要。

---

## Task 3.2 结论与政策启示同步改写
### 必须避免
- 不要再写“与一般数字叙事完全分离”。
- 不要再写“强因果识别”。
- 不要再写“信息供给效应占据主导”这种过满结论，除非新增证据真的更强。

### 建议写法
- “多种识别练习支持其因果解释。”
- “当前测度较一般数字叙事更贴近数据资源的实际使用、治理和价值实现，但边界仍需审慎界定。”

---

## Task 3.3 固定效应和表注口径统一检查
### 必做核查
- 所有表中关于 `Firm FE / Year FE / Ind×Year FE` 的行必须拆开写。
- 不允许再出现 `Firm/Year FE = YES` 这种混合标签。
- 正文描述、表头、表注三处必须完全一致。

---

# P4（标题任务，必须单独输出一份 memo）

## Task 4.1 输出中文标题候选 8 个
### 硬要求
1. 必须保留“数据要素”这个词。
2. 不再使用普通的“A对B的影响”。
3. 标题必须与结果闭环，不能问一个论文没回答的问题。
4. 方法最多放到副标题，不要让“大语言模型”压过研究问题本身。

### 分组输出
#### A. 稳健型（最适合投稿）
要求更克制，更贴实证边界。

#### B. 设问型（更有记忆点）
要求基于已有 `WashGap / DUllm_lenstd` 结果，围绕“概念提及 vs 实质利用”。

#### C. 数据要素闭环型（只有在 `DUevent / DUchain / DUcore` 有结果时才可用）
围绕“业务嵌入”“治理闭环”“价值实现”。

### 至少包含以下方向的候选
- `概念提及还是实质利用？——数据要素利用与股价延迟`
- `数据要素利用的信息价值：来自股价延迟的证据`
- `数据要素利用的广与深——来自年报文本与股价延迟的证据`
- `数据要素利用的“广而浅”与“深而实”：来自股价延迟的证据`

---

## Task 4.2 输出英文标题候选 5 个
### 要求
- 主标题尽量 sober，不要太花。
- 可保留 `data element`，但要确保副标题或正文能兜住这个表述。

### 至少包含三类版本
1. 保守型：
   - `Data-Element Utilization and Stock Price Delay: Evidence from Annual-Report Text`
2. 广度—深度型：
   - `Breadth or Depth? Data-Element Utilization and Stock Price Delay`
3. 实质利用型（仅当新增指标支持时）：
   - `Substantive Data-Element Utilization and Stock Price Delay`

---

# 交付物清单（必须全部输出）
1. 一份新的变量说明文档：
   - `docs/v15_measurement_upgrade.md`
2. 一份新的标题 memo：
   - `docs/v15_title_options.md`
3. 一份新的 LLM 复核附录草稿：
   - `docs/v15_llm_reproducibility_appendix.md`
4. 新增变量数据表：
   - `DUevent`
   - `DUchain_count`
   - `DUclosedloop`
   - `DUcore`
   - 章节限定指标
5. 一套更新后的回归表（主文 + 附录）
6. 一版修订后的 `v15` 稿件
7. 一份 `CHANGELOG_v14_to_v15.md`

---

# 最终验收标准

## A. 构念层面
- 至少有 **1 个新增测度**（`DUevent` / `DUchain` / `DUcore` / 章节限定口径）在滞后规格下对 `PriceDelay` 稳健显著为负。
- 至少有 **1 个新增测度** 比 `DUkw` 更能摆脱 `GenericNarr` 的共线性吞噬，或者至少在解释上更接近“数据要素利用”。

## B. LLM 层面
- 原始上下文级日志可追溯。
- 替代聚合指标能重建。
- cross-model validation 与 human adjudication 被清楚区分。

## C. 文稿层面
- 摘要、Highlights、引言、理论、测度、结论的口径完全一致。
- 不再过度宣称“与一般数字叙事完全分离”。
- 表格标签和正文描述不再冲突。

## D. 标题层面
- 中文标题保留“数据要素”。
- 标题不再是普通的“A 对 B 的影响”。
- 标题与结果闭环，能直接被正文支持。

---

# 建议执行顺序（严格按顺序）
1. `Task 0.1` 重建上下文级日志
2. `Task 0.2` 做 `DUevent`
3. `Task 0.3` 做 `DUchain`
4. `Task 0.4` 做 `DUcore`
5. `Task 0.5` 做章节限定口径
6. `Task 1.1` 替代聚合
7. `Task 1.2` cross-model validation
8. `Task 1.3` 人类复核
9. `Task 2.1`～`2.4` 新意扩展
10. `Task 3.1`～`3.3` 文稿同步
11. `Task 4.1`～`4.2` 标题 memo

---

# 给 Codex 的一句话指令
不要再用“更多稳健性”替代“更好的构念”。这轮的第一目标不是多跑几张表，而是把“数据要素利用”四个字在测度上往前推进半步。
