# v15 LLM 可复核性附录草稿

## 当前已完成

- 评分模型、温度参数、评分提示词和 `0/1/2/3` rubric 已在主文附录中给出。
- 已生成 `120` 条分层复核样本，并在主文附录中说明分层逻辑与真实示例。
- 已报告 `DU_llm_lenstd` 这一长度标准化语义深度指标，用于缓解篇幅污染。

## 当前仍未完成

- 原始上下文级逐条评分日志尚未完整归档，因此：
  - 不能从本地直接重建 `mean score`
  - 不能从本地直接重建 `share(score>=2)`
  - 不能直接做严格意义上的聚合方式稳健性总表

## v15 的处理原则

- 不把尚未完成的工作写成已经完成。
- 不把第二个模型复核写成人工复核。
- 在正文中保持以下表述：
  - 现阶段 `DU_llm` 与 `DU_llm_lenstd` 主要承担 semantic validation 角色。
  - 对构念独立性与可复核性的表述保持审慎。

## 后续最优先任务

1. 重新归档上下文级评分日志。
2. 在统一 rubric 下做 cross-model validation。
3. 对最难样本做轻量 human adjudication。
4. 重建 `mode / mean / share(score>=2)` 三类聚合。

## 可直接用于正文的谨慎写法

“受限于项目目录中未完整保留上下文级逐条评分日志，本文目前无法重建基于 mean score 或 share(score>=2) 的全样本替代聚合。因而，v15 将 `DU_llm` 与 `DU_llm_lenstd` 主要定位为语义深度的支持性测度，并通过评分模板、真实上下文示例和分层复核样本设计提高其可复核性。” 
